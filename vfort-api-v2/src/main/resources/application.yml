server:
  port: 8070                          # Runs alongside Flowable on 8080
  servlet:
    context-path: /vfort              # All endpoints: http://localhost:8090/vfort/api/...



spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: badal123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  # ── Oracle DB (your business data) ──
#  datasource:
#  autofix.datasource.url=jdbc:postgresql://localhost:5432/postgres
#  autofix.datasource.username=postgres
#  autofix.datasource.password=badal123
#  autofix.datasource.schema=prdoms
#  url: jdbc:postgresql://localhost:5432/postgres
#  username: postgres
#  password: badal1schema23
#  schema: prdoms

#  datasource:
#    url: jdbc:oracle:thin:@PRODOMS
#    username: PRD_READ
#    password: LOCK_DN2021
#    driver-class-name: oracle.jdbc.OracleDriver
#    hikari:
#      connection-timeout: 30000
#      maximum-pool-size: 10

#  jpa:
#    hibernate:
#      ddl-auto: none                  # Never auto-create Oracle tables
#    database-platform: org.hibernate.dialect.Oracle12cDialect
#    show-sql: false

  # ── CORS (allow React frontend) ──
  mvc:
    cors:
      allowed-origins: "http://localhost:5173"

# ── Flowable REST connection ──
flowable:
  rest:
    base-url: http://localhost:8090/flowable
    username: admin
    password: admin123

# ── JWT config ──
jwt:
  secret: 9fR7xW2kM5vN8zP3qB6hK1tJ4wD9cX8mZ2vL5nQ7rT0bY3pS6wK9fM1vN4zP7qB3
  expiration-ms: 900000            # 15 minutes

# ── Logging ──
logging:
  level:
    com.vfort: DEBUG
    org.springframework.web: INFO
